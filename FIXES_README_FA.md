# راهنمای سریع: چه چیزهایی اصلاح شد و چگونه استفاده کنی

تاریخ: 2026-08-28
این بسته اصلاحاتِ باگ‌های داشبورد AHRAM LIVE5 است. فقط فایل‌های زیر عوض شده‌اند؛ بقیهٔ فایل‌ها دست نخورده‌اند.

---

## فایل‌هایی که باید جایگزین کنی

| فایل | وضعیت | کار |
|------|-------|-----|
| `tsetmc_collector_v5.py` | **جدید** | جمع‌آوری خودکفا از TSETMC (جایگزین collector قدیمی که نداشتی) |
| `jalali_v5.py` | **جدید** | تبدیل تاریخ شمسی برای محاسبهٔ سررسید |
| `ahram_engine_v5.py` | **جدید** | موتورِ کاملِ خودکفا: جمع‌آوری + ۶ موتور V2 + ساخت داشبورد |
| `connect_strategy_dashboard_v5.py` | **اصلاح** | دیگر قیمت زنده را بازنویسی نمی‌کند؛ کارت‌ها هر ۶۰ ثانیه آپدیت می‌شوند؛ تنه�ا LIVE5 می‌سازد |
| `config.py` | **اصلاح** | افزودن نماد «فملی» + یکدست‌کردن queue_gap |
| `strategy_bridge_v5.py` | **اصلاح** | افزودن «فملی» به دیتابیس‌ها |
| `START_LIVE5_ENGINE.bat` | **اصلاح** | حالا موتورِ خودکفا را اجرا می‌کند |
| `START_LIVE5_DASHBOARD.bat` | **اصلاح** | بررسي وجود فایل و باز کردن صفحه |
| `options_dashboard_AHRAM_LIVE5.html` | **نمونه** | یک خروجیِ تازه‌ساخته (می‌توانی خودت هم بسازی) |

---

## باگ‌هایی که حل شد

1. **قیمت زنده بازنویسی نمی‌شود** — اسکریپتِ تزریق‌شده دیگر `allStocksMap/allOptions/allPuts/liveOptionQuoteMode` را دستکاری نمی‌کند. (انگار که قبلاً قیمتِ لحظه‌ایِ داشبورد را با دادهٔ کهنهٔ دیتابیس می‌پوشاند و «مظنهٔ اجرایی» را خاموش می‌کرد.)
2. **کارت‌های بنفش V5** دیگر ثابت نیستند؛ هر ۶۰ ثانیه خودبه‌خود تازه می‌شوند (اگر صفحه از طریق http باز شود، JSON تازه می‌گیرد؛ اگر با file:// باز باشد، دادهٔ همان لحظه را نشان می‌دهد و زمانِ ساخت را می‌نویسد).
3. **`LIVE4_V5.html` یکسانِ `LIVE5.html`** دیگر تولید نمی‌شود — فقط `LIVE5.html` ساخته می‌شود تا گمراه نکنند.
4. **«فملی»** حالا به موتور و دیتابیس‌ها اضافه شد؛ از این به بعد هر ۴ نماد (اهرم، وبملت، شستا، فملی) کارت V5 دارند.
5. **وابستگی به فایل‌های قدیمیِ غایب** حذف شد — موتور حالا با `tsetmc_collector_v5` خودش داده را می‌گیرد و بدون collector/sترategy/option_selector کار می‌کند.
6. **`queue_gap`** بین config و موتور یکدست شد.
7. (**نکته**) دو تابع حد سود/ضرر در موتور قدیمی `ahram_pro_v5.py` بود که دیگر در مسیر اصلی به کار نمی‌رود؛ مسیر جدید از `decision_engine_v2` استفاده می‌کند.

---

## نحوهٔ استفاده (شروع ساده)

1. همهٔ فایل‌ها را در **یک پوشه** بگذار.
2. پایتون ۳.۱۰+ نصب باشد.
3. روی `START_LIVE5_ENGINE.bat` دوبار کلیک کن.
   - یک سیکل اجرا می‌شود: از TSETMC می‌گیرد → ۶ موتور V2 + ترس‌وطمع → `ahram_strategy_data_v5.json` → `options_dashboard_AHRAM_LIVE5.html`.
   - مرورگر خودش صفحه را باز می‌کند.
4. برای **اجرای دائمیِ هر ۵ دقیقه**: به‌جای یک سیکل، در ترمینال بزن:
   ```
   python ahram_engine_v5.py
   ```
   (یا قبل از `--once` آن را حذف کن).

> اگر `--offline` را اضافه کنی، به‌جای شبکه از `sample_marketwatch.txt` استفاده می‌کند (برای تست/بدون بازار).

---

## نکته‌ها

- سندباکسِ من به TSETMC دسترسی ندارد (فیلتر خروجی)، پس **قیمتِ واقعی فقط روی سیستم خودت (داخل ایران) قابل تأیید است**. پارسِ داده و کلِ خط تولید (تا ساخت HTML و رندر کارت‌ها) را با نمونهٔ ضبط‌شده تست کردم و سبز است.
- فایل‌های `ahram_v2.db`, `webmellt.db`, `shasta.db` که در ریپو هست جدول‌های `prices` و `options` ندارند؛ جمع‌آورِ جدید هنگام اولین اجرا **خودش این جدول‌ها را می‌سازد** و پر می‌کند.
- موتور قدیمی `ahram_pro_v5.py` (که به فایل‌های غایب وابسته است) دیگر در مسیر اصلی استفاده نمی‌شود؛ می‌توانی آن را نگه داری، ولی `START_LIVE5_ENGINE.bat` حالا از `ahram_engine_v5.py` استفاده می‌کند.
